services:
  bot:
    build: .
    network_mode: host
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - ALLOWED_USERS=${ALLOWED_USERS}
    volumes:
      - ./logs:/app/logs